#include <stdio.h>

int main() {
  int s=0;
  int n;
  printf("Enter the value of n:");
  scanf("%d",&n);
  for(int i=1;i<=n;++i)
  {
      s=s+i;
  }
  printf ("\nThe sum is :%d",s);
  return 0;
  
  
  
   
}
