#include <stdio.h>

int main() {
   char str[100];
   printf("Enter the string");
   scanf("%s",str);
   int c=0;
   int d=0;
   for(int i=0;str[i]!='\0';++i)
   {
     c=i;
     
   }
   
   for(int i=c;i>=0;--i)
   {
       printf("%c",str[i]);
   }
   
   
   
    return 0;
}
