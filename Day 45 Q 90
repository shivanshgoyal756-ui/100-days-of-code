#include <stdio.h>
int main()
{
  char str[100];
  printf("enter the string");
  scanf("%[^\n]",str);
  for(int i=0;str[i]!='\0';++i)
  {
    if(str[i]>=97&&str[i]<=122)
    str[i]=(char)((int)str[i]-32);
    else
     str[i]=(char)((int)str[i]+32);
  }
  printf("\n %s",str);
}
