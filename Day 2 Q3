#include <stdio.h>

int main() {
   int l,b;
   printf("The length and breadth :");
   scanf("%d%d",&l,&b);
   int a=l*b;
   printf("\nthe area is %d",a);
   int p=2*(l+b);
   printf("\n the perimeter  is %d",p);
  
    return 0;
}
