#include <stdio.h>
int main()
{
int n;
int a=3;
float sum=0.0;
printf("Enter the value of n");
scanf("%d",&n);
for(int i=2;i<=n*2;i=i+2)
{
    sum=sum+(float)i/a;
    a=a+4;
}
printf("the sum is : %f",sum);
return 0;
}
    
