#include <stdio.h>
int main()
{
   int num=0;
   int sum=0;
   printf("Enter the number");
   scanf("%d",&num);
   while(num>0)
   {
      int d=num%10;
      if(d%2!=0)
      sum=sum+d;
      num=num/10;
   }
   printf("The sum is: %d",sum);
   return 0;
}
