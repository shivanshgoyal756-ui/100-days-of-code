#include <stdio.h>
int main()
{
  char str[100];
  char ch;
  printf("enter the string");
  scanf("%[^\n]",str);
  printf("\nEnter the character to be found");
  scanf("%c",&ch);
  int c=0;
  for(int i=0;str[i]!='\0';++i)
  {
    if(str[i]==ch)
    ++c;
  }
  printf("\nthe frequency is %d",c);
}
