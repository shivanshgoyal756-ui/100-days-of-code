#include <stdio.h>
void rev(int num)
{
    int neww=0;
    while(num>0)
    { 
        neww=neww*10+(num%10);
        num=num/10;
    }
    printf("Reverse is%d",neww);
    
    
}

int main() {
    int num;
    printf("Enter the number");
    scanf("%d",&num);
    rev(num);
    return 0;
}
